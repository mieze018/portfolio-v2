name: Cypress Tests
on: [push]

# Why: GITHUB_TOKEN の権限を最小化してサプライチェーン攻撃リスクを低減する。
#      GITHUB_TOKEN は Cypress Cloud のラン識別に使用するだけで書き込み不要なため
#      contents: read で十分。
permissions:
  contents: read

jobs:
  cypress-run:
    runs-on: ubuntu-latest
    # Runs tests in parallel with matrix strategy https://docs.cypress.io/guides/guides/parallelization
    # https://docs.github.com/en/actions/using-jobs/using-a-matrix-for-your-jobs
    # Also see warning here https://github.com/cypress-io/github-action#parallel
    strategy:
      fail-fast: false # https://github.com/cypress-io/github-action/issues/48
      # matrix:
      #   containers: [1, 2] # Uses 2 parallel instances
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: '.node-version'
          cache: 'pnpm'
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Install Cypress binary
        # Why: pnpm install はキャッシュヒット時に postinstall をスキップするため、
        # Cypress バイナリが存在しない場合がある。明示的にインストールして保証する。
        run: pnpm exec cypress install
      - name: Cypress run
        # Uses the official Cypress GitHub action https://github.com/cypress-io/github-action
        uses: cypress-io/github-action@v4
        with:
          install: false
          browser: chrome
          # Starts web server for E2E tests - replace with your own server invocation
          # https://docs.cypress.io/guides/continuous-integration/introduction#Boot-your-server
          build: pnpm build
          start: pnpm start --port 3009
          wait-on: 'http://localhost:3009' # Waits for above
          # Records to Cypress Cloud
          # https://docs.cypress.io/guides/cloud/projects#Set-up-a-project-to-record
          record: true
          parallel: true # Runs test in parallel using settings above
        env:
          # For recording and parallelization to work you must set your CYPRESS_RECORD_KEY
          # in GitHub repo → Settings → Secrets → Actions
          CYPRESS_RECORD_KEY: ${{ secrets.CYPRESS_RECORD_KEY }}
          # Creating a token https://docs.github.com/en/authentication/keeping-your-account-and-data-secure/creating-a-personal-access-token
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # vercelで設定してるenv GitHubのシークレットにも追加する
          NEXT_PUBLIC_api_Key: ${{ secrets.NEXT_PUBLIC_api_Key }}
          NEXT_PUBLIC_NOTION_TOKEN: ${{ secrets.NEXT_PUBLIC_NOTION_TOKEN }}
          NEXT_PUBLIC_FORM: ${{ secrets.NEXT_PUBLIC_FORM}}
