
#!/bin/sh
# Why: Biome に移行済み。check:ci は --write なしで差分を出さずに検査のみ行う
pnpm check:ci
# Why: lat.md ナレッジグラフのwikiリンクとコード参照の整合性を検証する
pnpm lat:check
# Why: 未使用 export / ファイル / 依存パッケージを可視化する（|| true で非ブロッキング）
#      現時点で既存の指摘が残っているため push をブロックしない。全件解消後に || true を外す。
pnpm unused || true
# pnpm cy:test

# Why: push時の自動アップグレードはパッケージの意図しない升級を招くため無効化
# Alternative: 依存関係の更新はrenovate/dependabotまたは手動で管理する
# pnpm upgrade:linter
# pnpm upgrade:types
# pnpm upgrade:storybook
# pnpm up chromatic
