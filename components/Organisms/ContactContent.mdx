import { Meta, ArgsTable } from '@storybook/blocks'
import { ContactContent } from './ContactContent'

# ContactContent

コンタクトページのコンテンツ

コンタクトフォームを表示する

仕事の受付状況をNotionAPIから日本語英語それぞれで取得して環境に応じて表示する。

TODO: テストできないのでロジックを切り離してPropを渡す形にする。

<ArgsTable of={ContactContent} />
