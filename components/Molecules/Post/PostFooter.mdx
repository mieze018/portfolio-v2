import { Meta, ArgsTable } from '@storybook/blocks'
import { PostFooter } from './PostFooter'
import * as PostFooterStories from './PostFooter.stories'

<Meta of={PostFooterStories} />

# PostFooter

投稿の日時を表示する
ISOフォーマットを`January 2021`のようなフォーマットに変換する

<ArgsTable of={PostFooter} />
