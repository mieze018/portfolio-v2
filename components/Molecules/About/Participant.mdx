import { Meta, ArgTypes } from '@storybook/blocks'
import { Participant } from './Participant'
import * as ParticipantStories from './Participant.stories'

<Meta of={ParticipantStories} />

# Participant

参加中のイベントやコンペティションを表示するためのコンポーネント

## Props

<ArgTypes of={Participant} />
