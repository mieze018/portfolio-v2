import { Meta, ArgTypes } from '@storybook/blocks'
import { Nav } from './Nav'
import * as NavStories from './Nav.stories'

<Meta of={NavStories} />

# Nav

ナビゲーションメニューのラッパー

スクロールするとスタイルが変わる

<ArgTypes of={Nav} />
